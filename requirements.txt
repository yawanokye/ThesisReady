# ProjectReady AI - stable Render build requirements
# Keep this file to top-level app dependencies only.
# Do not pin transitive packages such as starlette, httpx, pydantic-core,
# anyio, httptools, uvloop, watchfiles, or websockets unless there is a clear need.

fastapi==0.115.12
uvicorn==0.34.2
pydantic==2.11.5
python-dotenv==1.1.0

# OpenAI SDK is also used for DeepSeek because DeepSeek supports OpenAI-compatible API calls.
openai==1.82.0

python-docx==1.1.2
jinja2==3.1.6
python-multipart==0.0.20
requests>=2.28.0

# Chapter payments and Render PostgreSQL
stripe>=11.0.0
psycopg2-binary>=2.9.9

pypdf==5.6.0
openpyxl==3.1.5
